{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the DeepSeek Harness desktop window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
